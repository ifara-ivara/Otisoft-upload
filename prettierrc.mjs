const config = {
  semi: true,
  trailingComma: "es5",
  singleQuote: false,
  printWidth: 100,
  tabWidth: 2,
  useTabs: false,
  endOfLine: "lf",
  ignorePaths: ["node_modules", "dist", "build"],
  ignorePatterns: ["**/*.test.ts", "**/*.spec.ts", "**/*.stories.tsx"],
};

export default config;
